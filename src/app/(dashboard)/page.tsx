export default function DashboardHome() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <p className="text-sm text-muted-foreground">
        Pick a module from the sidebar. Each module implements one of the four Phase-1
        processes documented in docs/process-flows/.
      </p>
    </div>
  );
}
